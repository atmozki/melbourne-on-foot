name: melbourne_on_foot
version: "1.0.0"
profile: melbourne_on_foot

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: target
clean-targets: ["target", "dbt_packages"]

models:
  melbourne_on_foot:
    staging:
      +materialized: view
    marts:
      +materialized: table
